<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">


    <android.support.design.widget.TabLayout
        android:id="@+id/tabs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:background="@color/BlueThemeColor"
        app:tabGravity="fill"
        app:tabIndicatorColor="@color/BrushedGold"
        app:tabMode="fixed"
        app:tabSelectedTextColor="@color/BrushedGold"
        app:tabTextColor="@color/white" />

    <android.support.v4.view.ViewPager
        android:id="@+id/viewpager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/button_pdf"
        android:layout_below="@+id/tabs">

    </android.support.v4.view.ViewPager>

    <Button
        android:id="@+id/button_pdf"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@color/BlueButtonBackGroundColor"
        android:text="@string/monthly_list"
        android:textAllCaps="false"
        android:textColor="@color/BlueButtonTextColor" />


</RelativeLayout>